# kb-cli

_Map of Content for your knowledge base. Auto-generated by `vaultforge`._

**6** concepts · **0** sources · **21** tags

## Concepts

- [[concepts/architecture-overview|Architecture Overview]] — High-level map of vaultforge — an LLM-powered knowledge base engine that drives Claude Code as a subprocess to research, compile, and maintain Obsidian-flavored Markdown wikis.
- [[concepts/claude-runner|Claude Runner]] — Spawns the `claude` CLI as a subprocess, parses its line-delimited stream-json output into typed `ProgressEvent`s, and delivers them either to stdout or to a caller-provided callback.
- [[concepts/cli-commands|CLI Commands]] — Commander-based CLI entry point and the nine subcommand handlers that translate user intent into calls into the core engine.
- [[concepts/content-pipeline|Content Pipeline]] — The wiki-generation orchestrators — `WikiCompiler`, `Ingestor`, `RepoIngestor`, `scanRepo`, and `rebuildIndex` — that turn user commands into subprocess calls and then deterministically rebuild the Map of Content.
- [[concepts/prompts|Prompts]] — Pure functions that build the text prompts for every Claude invocation — research, compile, ask, health, URL fetch, and repo documentation. No I/O, no side effects.
- [[concepts/session-management|Session Management]] — Owns the session lifecycle — creation, listing, switching, active-session lookup — and the global config at ~/.vaultforge/config.json.

## Summaries

_No source summaries yet._

## Tags

#architecture #claude #cli #commander #compiler #entrypoint #indexer #ingestor #llm #overview #persistence #pipeline #prompts #public-api #pure-functions #repo #sessions #state #stream-json #subprocess #vaultforge
